{
  "summary": "CLOUUD Genesis E2E-001 wizard tested end-to-end via UI. All 8 stages executed successfully through the frontend, backed by the FastAPI backend endpoints. Final E2E Validation Report on-screen shows PASS for every stage. Backend /api/v1/transactions verified via curl.",
  "tested_scope": ["frontend", "backend", "integration"],
  "passed": [
    "Backend POST /api/v1/transactions returns 200 with transaction_id",
    "Stage 1 UI: Execute Transaction creates tx and advances wizard",
    "Stage 2-4 UI: Trigger Proof Generation completes (LLM or mock fallback) and auto-advances to Verify",
    "Stage 5-6 UI: Verify Proof Commitment shows 'Verification Successful' (valid=true, commitment_match=true)",
    "Stage 8 UI: Publish to Base/Ethereum returns chain anchor and auto-advances to Tamper step",
    "Stage 7 UI: Modify DB Amount & Verify shows 'TAMPER DETECTED' with valid=false, commitment_match=false",
    "Final E2E Validation Report renders with all 8 stages marked PASS"
  ],
  "failed": [],
  "action_items": [],
  "success_rate": {"backend": "100%", "frontend": "100%"},
  "retest_needed": false,
  "full_testing_deferred": true,
  "notes": "LLM key budget fallback confirmed working - proof generation completed within ~30s using default reasoning states when needed. No error messages on any page. No console/runtime errors surfaced."
}
